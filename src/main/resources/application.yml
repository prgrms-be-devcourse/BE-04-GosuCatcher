server:
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  jwt:
    accessTokenSecretKey: DUMMY_ACCESS_TOKEN_SECRET
    refreshTokenSecretKey: DUMMY_REFRESH_TOKEN_SECRET
    accessTokenExpiredTime: 1
    refreshTokenExpiredTime: 30
  config:
    import:
      - classpath:application-aws.yml
      - classpath:application-db.yml

  mail:
    host: smtp.gmail.com
    port: 587
    username: DUMMY_EMAIL_ADDRESS
    password: DUMMY_PASSWORD
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            enable: true

  redis:
    host: 0.0.0.0
    port: 6379

member:
  profile:
    root: DUMMY_ROOT

secret:
  coolsms:
    apiKey: DUMMY_KEY
    apiSecret: DUMMY_SECRET
    fromNumber: DUMMY_PHONE_NUMBER
    domain: https://api.coolsms.co.kr
    expirationTime: 600

auth:
  senderEmail: DUMMY_EMAIL_ADDRESS
  expirationTime: 300
